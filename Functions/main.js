"use strict";
exports.__esModule = true;
// * function that take string as a prameter and return value as string
function displayName(name) {
    // console.log(name);
    return name;
}
// * function that take number as a prameter and doesn't return value
function someLogicFunction(num) {
    // console.log(name);
    if (num > 10) {
        console.log("WOW");
    }
}
